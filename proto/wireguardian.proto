syntax = "proto3";

package platform;

import "config.proto";

option go_package = "proto/pb";

message BuildVPNRequest { Config config = 1; }
message BuildVPNResponse { Config config = 1; }

service WireguardianService {
  rpc BuildVPN(BuildVPNRequest) returns (BuildVPNResponse);
}
