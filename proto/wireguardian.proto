syntax = "proto3";

package platform;

import "config.proto";

option go_package = "proto/pb";

message Status { bool success = 1; }

service WireguardianService { rpc BuildVPN(Project) returns (Status); }
