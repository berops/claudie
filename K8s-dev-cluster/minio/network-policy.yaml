kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: minio-only-allow-this-namespace
spec:
  podSelector:
    matchLabels:
      app: minio
  ingress:
  - from:
    - podSelector: {}