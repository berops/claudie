kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: minio-only-allow-this-namespace
  namespace: claudie
spec:
  podSelector:
    matchLabels:
      app: minio
  ingress:
  - from:
    - podSelector: {}