---
- name: Install wireguard
  apt:
    name: wireguard
    update_cache: yes

- name: Install pip
  apt:
    name: python3-pip
    update_cache: yes

- name: install wireguard via pip
  pip:
    name: wireguard
