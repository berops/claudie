apiVersion: apps/v1
kind: Deployment 
metadata:
  name: context-box
  labels:
    app: context-box
spec:
  replicas: 1
  selector:
    matchLabels:
      app: context-box
  template:
    metadata:
      labels:
        app: context-box
    spec:
      containers:
      - name: context-box
        image: eu.gcr.io/platform-infrastructure-316112/platform/context-box:test-tag 
        env:
        - name: DATABASE_PORT
          value: "27017"
        - name: DATABASE_HOSTNAME
          value: "mongodb://mongodb"
        - name: CONTEXT_BOX_PORT
          value: "50055"
        - name: CONTEXT_BOX_HOSTNAME
          value: "0.0.0.0"
        ports:
        - containerPort: 50055
---
kind: Service
apiVersion: v1
metadata:
  name: context-box
spec:
  selector:
    app: context-box
  ports:
  - protocol: TCP
    port: 50055
    targetPort: 50055