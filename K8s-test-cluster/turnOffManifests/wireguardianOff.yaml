apiVersion: apps/v1
kind: Deployment 
metadata:
  name: wireguardian
  labels:
    app: wireguardian
spec:
  replicas: 0
  selector:
    matchLabels:
      app: wireguardian
  template:
    metadata:
      labels:
        app: wireguardian
    spec:
      containers:
      - name: wireguardian
        image: eu.gcr.io/platform-infrastructure-316112/platform/wireguardian:latest 
# No Service needed