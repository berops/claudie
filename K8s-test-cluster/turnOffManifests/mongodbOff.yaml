apiVersion: apps/v1
kind: Deployment 
metadata:
  name: mongodb
  labels:
    app: database
spec:
  replicas: 0
  selector:
    matchLabels:
      app: database
  template:
    metadata:
      labels:
        app: database
    spec:
      containers:
      - name: mongodb
        image: mongo